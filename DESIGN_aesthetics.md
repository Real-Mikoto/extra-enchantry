# Extra Enchantry 美化设计方案（特效 / 音效 / 视觉反馈）

> 面向 27 个附魔 + 事件体系的视听反馈设计。基于 v1.1.0-alpha 代码盘点（2026-09-06）。
> 目标：**每个附魔触发时玩家"看得见、听得见、感觉得到"**，且不刷屏、不掉帧。

---

## 一、设计原则

### 1. 三层反馈体系（按信息重要性分层，绝不越级）

| 层级 | 定义 | 表现预算 | 例子 |
| --- | --- | --- | --- |
| **L1 常驻暗示** | 状态存在期间的环境级提示 | ≤2 粒子/秒，无音效或极低频 | 疾风脚下微光、御风滑翔气流 |
| **L2 触发反馈** | 效果生效的瞬间确认 | 3~16 粒子 + 1 音效 | 冲阵撞击、壁垒挡下重击 |
| **L3 重大时刻** | 斩杀 / 免死 / 挑战胜负 | 20~40 粒子 + 标志性音效 + 可叠加屏幕级表现 | 断罪斩杀、余烬免死、诸界浩劫清波 |

**越级禁令**：L2 效果不得使用 L3 的表现量（否则玩家对"真正重要的时刻"脱敏）。

### 2. 视听语言一致性

每个附魔锁定一组 **主题粒子族 + 音效族 + 色调**，全模组内不串味：

| 主题族 | 粒子 | 音效基调 | 归属附魔 |
| --- | --- | --- | --- |
| 灵魂/幽冥 | SOUL、SOUL_FIRE_FLAME | 低沉、灵魂沙系 | 断罪、余烬、蚀命、诸界浩劫 |
| 雷光 | ELECTRIC_SPARK、END_ROD | 高频金属/雷鸣 | 霆霓、假象、坠星 |
| 锋刃 | SWEEP_ATTACK、CRIT | 挥砍系（变调） | 破阵、藏锋、冲阵、触及 |
| 自然/生命 | HEART、HAPPY_VILLAGER、COMPOSTER | 柔和、上扬 | 汲取、活力、丰壤、庇护 |
| 水/深渊 | BUBBLE、SPLASH、GLOW_SQUID_INK | 湿润、闷响 | 渊息 |
| 风/空 | CLOUD、POOF | 呼啸 | 御风、空跃、疾风 |
| 守护 | ENCHANTED_HIT、WAX_ON 质感 | 厚重盾音 | 壁垒、坚壁、不屈、誓约 |
| 火/熔岩 | FLAME、LAVA | 燃烧 | 炽焰行者、余烬（辅） |

### 3. 性能与体验红线

* 一切粒子走**服务端 `sendParticles`**（自动广播），不引入自定义粒子类型（v1 不碰客户端注册）
* 单次触发粒子 ≤40；持续性效果按 tick 间隔节流（≥10 tick 一次）
* 音效默认 `SoundSource.PLAYERS`，音量 ≤1.0；**等级变调惯例**：I/II/III 级音调 1.0 / 1.1 / 1.2（已用于藏锋 1.4 属特例——拔刀斩是 L2+ 表现）
* 同一玩家 5 tick 内同种触发只播一次（防群体战刷屏，破阵/霆霓连锁尤其需要）

---

## 二、现状盘点（27 附魔 × 视听覆盖）

| 附魔 | 触发粒子 | 触发音效 | 持续表现 | 评价 |
| --- | --- | --- | --- | --- |
| 凋零保护 | — | — | — | ❌ 缺 L2（效果触发时玩家无感） |
| 炽焰行者 | — | — | — | ❌ 缺 L1（岩浆行走应有火迹） |
| 破限 | 金色名称 + 锁定视觉 | WARDEN_EMERGE | ✅ | ✅ 完整 |
| 拓阶 | 金色名称 | — | — | ⚠️ 挖高级矿无确认感 |
| 触及 | — | — | — | ❌ 超远命中无反馈 |
| 假象 | ELECTRIC_SPARK | — | 诱饵实体 | ⚠️ 触发缺音 |
| 汲取 | — | — | — | ❌ 回血无感 |
| 蚀命 | — | — | — | ❌ 受害者不知被侵蚀 |
| 活力 | — | — | HUD 心形 | ⚠️ HUD 有，世界内无 |
| 壁垒 | — | — | — | ❌ **P0**——挡下 20 伤害的重击玩家毫无知觉 |
| 劫后余辉 | TOTEM + SOUL_FIRE | （图腾音） | 状态效果图标 | ⚠️ 可强化 |
| 誓约 | — | — | — | ❌ 死亡保留这么重要的效果毫无仪式感 |
| 空跃 | — | — | — | ❌ **P0**——手感附魔没有起跳反馈 |
| 破阵 | SWEEP + CRIT 环 | （横扫音） | — | ✅ 完整 |
| 御风 | — | — | — | ❌ 推进增强看不见 |
| 无踪 | — | — | — | ◻️ 刻意无声（潜行主题，保持） |
| 断罪 | SOUL 爆发 | — | — | ⚠️ L3 缺标志性音效 |
| 疾风 | — | — | — | ❌ 移速流应有足下生风 |
| 余烬 | SOUL_FIRE | ITEM_BREAK | — | ⚠️ 免死是 L3，可再加屏闪 |
| 冲阵 | — | — | — | ❌ **P0**——撞击附魔没有打击感 |
| 不屈 | — | — | — | ❌ 免疫破盾无反馈 |
| 庇护 | HEART + END_ROD | — | — | ⚠️ 光环脉冲缺低音 |
| 坚壁 | — | — | — | ❌ 减免触发无反馈 |
| 归羽 | — | 拾取音 | — | ⚠️ 飞回无轨迹 |
| 坠星 | END_ROD 星形 + FIREWORK | （爆炸音） | — | ✅ 完整 |
| 霆霓 | ELECTRIC_SPARK | TRIDENT_THUNDER | — | ✅ 完整 |
| 藏锋 | SWEEP | 横扫音 1.4 调 | — | ✅ 完整 |
| 渊息 | — | — | — | ❌ 新附魔零表现 |
| 丰壤 | — | — | — | ❌ 新附魔零表现 |

**结论：10 个完全空白、8 个半残、9 个完整/刻意留白。** 下面按优先级给方案。

---

## 三、P0——手感类（直接影响战斗体验的 4 个）

### 1. 冲阵 Shield Charge：撞击打击感

* **触发点**（`ShieldChargeManager.applyCharge` 命中成立时）：
  - 粒子：目标身上 `ENCHANTED_HIT` ×8（盾牌附魔质感）+ 冲击方向 `POOF` ×4
  - 音效：`SoundEvents.SHIELD_BLOCK` 音量 1.0、音调 0.8（沉钝的盾击声）叠 `PLAYER_ATTACK_KNOCKBACK`（击退哨音）
  - 击退落地（被击飞目标着地瞬间，可选）：`ANVIL_LAND` 音量 0.3——**强烈推荐**，撞飞落地的闷响是这个附魔的灵魂
* 等级变调：I/II/III → 0.8 / 0.85 / 0.9

### 2. 空跃 Skyward：起跳反馈（客户端）

* **触发点**（`LocalPlayerMixin` 空中起跳成功时）：
  - 粒子：脚下 `CLOUD` ×6（向下喷）+ `POOF` ×3
  - 音效：`ENDER_DRAGON_FLAP` 音量 0.4、音调 1.6（一声短促的"振翅"——比任何 UI 音都更像"又跳了一次"）
  - II 级三连跳：第二跳音调 1.5、第三跳 1.4，音高递减制造"阶梯感"

### 3. 壁垒 Bulwark：格挡确认（玩家最需要的"我活下来了"信号）

* **触发点**（`ExtraEnchantry.applyBulwarkCap` 实际钳制时——原伤害 > 上限才触发）：
  - 粒子：胸口 `ENCHANTED_HIT` ×6
  - 音效：`SoundEvents.SHIELD_BLOCK` 音调 0.6（比普通盾格挡更低沉，"重击被硬吃"的质感）
  - **动作栏提示**：`壁垒挡下了 X 点伤害`（仅当原伤害 − 钳制后 ≥ 4，避免小伤刷屏）——灰色斜体，与破限锁定提示同风格
* 这是全模组最重要的一个反馈：壁垒的价值完全建立在"玩家意识到自己被救了"上

### 4. 断罪 Judgement：斩杀仪式感升级（L3 补完）

* 现状只有 SOUL 粒子爆发，补：
  - 音效：`SoundEvents.SOUL_ESCAPE`（灵魂逸散——26.2 有此事件，与 SOUL 粒子同族）音量 1.0，或备选 `WARDEN_SONIC_BOOM` 音量 0.3 做"处决重音"
  - boss 双倍触发时：低音 `NOTE_BASS` 系或 `ELDER_GUARDIAN_CURSE` 音量 0.2 短促一下（区分"触发了 boss 变体"）
  - 斩杀者与被害者听到的音量应一致（服务端 playSound 广播天然满足）

---

## 四、P1——反馈类（8 个，每个 1~3 行改动量）

| 附魔 | 触发点 | 设计 |
| --- | --- | --- |
| 汲取 | hurtServer RETURN 回血时 | 攻击者身上 `HEART` ×2 + `NOTE_HARP` 音调 1.2 音量 0.3（轻到几乎下意识，回血不该吵） |
| 蚀命 | 百分比伤害结算时 | **受害者**身上 `SOUL` ×4 + `SOUL_ESCAPE` 音量 0.2——受害者视角才知道自己被侵蚀了（与断罪同族但量级减半，不串味） |
| 坚壁 | 不可格挡伤害减免成立时 | `ENCHANTED_HIT` ×4 + `SHIELD_BLOCK` 音调 1.2（比壁垒高——"魔法被弹开"vs"重击被硬吃"） |
| 不屈 | 破盾被免疫 / 时长减半时 | `ENCHANTED_HIT` ×4 + `SHIELD_BLOCK` 音调 1.0；免疫破盾那一下加 `ANVIL_PLACE` 音量 0.2（金属就位声="盾没脱手"） |
| 触及 | 命中距离 > 原版攻击距离的目标时 | 挥砍音照常，追加 `CRIT` ×3 沿攻击线 3 点排开（"剑气够到了"的延长线视觉）；无额外音效（触及是常态效果，不该每刀都响） |
| 归羽 | 箭矢起飞返还瞬间 | 箭位置 `END_ROD` ×3 拖尾 + `ITEM_PICKUP` 提前到起飞时（取代现在的到手才响——飞回过程 1 秒，声音先行更有"回来了"的期待感） |
| 渊息 | 入水且氧气 > 原版上限时 | 首次入水 `BUBBLE_COLUMN_UP` ×8 + `AMBIENT_UNDERWATER_ENTER`（原版入水环境音，最贴主题）；III 级水下开挖时挖掘粒子加 `BUBBLE` ×2/次（10 tick 节流） |
| 丰壤 | 双倍掉落触发时 | 作物位置 `HAPPY_VILLAGER` ×5 + `COMPOSTER_FILL_SUCCESS` 音调 1.2（丰收的"啵"）；3×3 范围收获时每格 1 个 `HAPPY_VILLAGER`（多格连响自然形成"哗啦一片"） |

---

## 五、P2——氛围类（7 个，润色性质）

| 附魔 | 设计 |
| --- | --- |
| 疾风 | 移速加成生效且移动中时：脚下 `CLOUD` ×1 / 15 tick（L1 常驻，III 级改 `POOF`，跑起来一缕烟——"足下生风"）；潜行时粒子也停（与效果同步） |
| 活力 | 装备穿上瞬间 `HEART` ×3 + `NOTE_HARP` 音量 0.2（一次性确认"上限涨了"）；不加常驻粒子（活力是被动数值，刷屏警告） |
| 誓约 | **重生仪式**：重生后誓约物品回插完成时 `TOTEM_OF_UNDYING` ×10 + `NOTE_CHIME` 音量 0.5（死亡没失去东西，值得一声清音）；仅部分誓约（非四件套）粒子减半 |
| 御风 | 烟花推进增强生效时（factor > 1）：滑翔者身后 `CLOUD` ×3 / 10 tick（加速气流尾迹，II 级白、III 级掺 `END_ROD` ×1——末地主题呼应） |
| 凋零保护 | 凋零效果被缩短结算时：`SPORE_BLOSSOM_AIR` ×4 + `NOTE_FLUTE` 音量 0.3（"毒素被净化"的轻盈感，与灵魂族区分） |
| 炽焰行者 | 岩浆上行走时：脚下 `FLAME` ×2 / 10 tick + 偶发 `LAVA` ×1（30 tick）——熔岩凝固成路的每一步都该有火迹 |
| 拓阶 | 成功挖掘"本不可挖"的方块时（boostedHarvestable 路径）：`ENCHANTED_HIT` ×3 + `ANVIL_USE` 音量 0.15（极轻的金属音="这镐子不对劲，是附魔在干活"）；正常挖掘不触发（防刷屏的关键） |

---

## 六、通用基础设施（先做这两个，后面全是填表）

### 1. `FxHelper` 工具类（新建）

收编全模组的粒子/音效模板，消灭各处散写的 sendParticles 坐标算术：

```
FxHelper.ring(level, pos, radius, particle, count)      环（破阵已有，收编）
FxHelper.burst(level, entity, particle, count, spread)  实体中心爆发
FxHelper.trail(level, from, to, particle, count)        线段拖尾（触及剑气/归羽轨迹）
FxHelper.playAt(level, pos, sound, source, vol, pitch)  音效 + 变调
FxHelper.pitchForLevel(level)                           等级变调 1.0/1.1/1.2
FxHelper.throttle(uuid, key, minIntervalTicks)          同种触发节流（防刷屏）
```

### 2. 触发节流器

`FxHelper.throttle`：每玩家每效果键 5 tick 内只放行一次——破阵溅射 3 目标、丰壤 3×3、霆霓连锁这类"一次动作多次结算"的场景必备。CleaveManager 的 ThreadLocal 模式是判定防递归，这个是**表现层**节流，两者不冲突。

### 3.（可选，L3 专属）音效双通道

重大时刻（断罪斩杀 / 余烬免死 / 诸界浩劫胜负）同时播"近场音 + 低频垫底"（如 SOUL_ESCAPE + NOTE_BASS 0.1 音量），营造厚度。只许 L3 用。

---

## 七、里程碑

| 阶段 | 内容 | 验收 |
| --- | --- | --- |
| **fx-1** | FxHelper + 节流器；P0 四项（冲阵/空跃/壁垒/断罪） | 每个 P0 触发可见可闻；壁垒动作栏不刷屏 |
| **fx-2** | P1 八项 | 全部触发确认；节流在群战下不刷屏 |
| **fx-3** | P2 七项 + 双通道打磨 | 持续效果帧率无感（粒子预算内） |

**明确不做**：自定义粒子类型注册、着色器/屏幕震动、动态光照（客户端渲染成本高且易与优化模组冲突）；音效全部复用原版 SoundEvents（保持"原版+风格"，不引资源包音源）。

**风险点**：`SoundEvents.SOUL_ESCAPE` / `ENDER_DRAGON_FLAP` / `AMBIENT_UNDERWATER_ENTER` 等常量名需按 mc-fabric-mod-api-verify 流程在 26.2 反编译确认后再写码（SoundEvents 各字段类型有 SoundEvent 与 Holder 两种形态，playSound 重载要对应）。
